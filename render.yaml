services:
  - type: web
    name: intelligent-sales-forecasting
    env: python
    buildCommand: pip install -r requirements.txt && python train_models.py
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
